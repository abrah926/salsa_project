import React from 'react';
import Hero from "../components/Hero";

const Home = () => {
  return (
    <div>
      {/* Include the Hero component */}
      <Hero />
    </div>
  );
};

export default Home;
